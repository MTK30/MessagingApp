normal-dispatcher {
    type = Dispatcher
    executor = "fork-join-executor"
    fork-join-executor {
        parallelism-min = 60
        parallelism-factor = 10.0
        parallelism-max = 500
    }
    throughput = 200
}